Вопрос:

	Что выведет данная программа и почему?

func main() {
  n := 0
  if true {
     n := 1
     n++
  }
  fmt.Println(n)
}

Ответ:

    0

Переменная n была переобъявлена внутри блока if, но она не существует вне блока, поэтому значение 0 не изменилось.